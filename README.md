# TypeScript: Absolute path imports

Sample project for use Babel + TypeScript for use absolute path to import modules.

## Setup Environment

```bash
# Install dependencies
npm i

# Build JS code from TS
npm run build

# Start JS code
npm run start:prod
```
